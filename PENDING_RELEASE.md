# מקור האמת לגרסה — `1.0.9+68`

> אם יש סתירה בין הטלפון / הצ׳אט / הזיכרון לבין המסמך הזה — **רק המסמך + `git` קובעים**.

---

## הזהות של הגרסה (חובה לבניה)

| שדה | ערך מדויק |
|-----|-----------|
| **גרסה לבניה / חנויות / TestFlight** | **`1.0.9+68`** |
| **`mobile/pubspec.yaml` → `version`** | `1.0.9+68` |
| **מספר build** | `68` |
| **שם שיווקי** | 1.0.9 |
| **ענף** | `main` |
| **קומיט התוכן (`+68`)** | `0be65b3` |
| **גרסה קודמת (`+67`)** | `e7945be` (+ FCM prune `6631fc0`) |
| **מצב נוכחי** | **נעולה כגרסת היעד אחרי הפיילוט** — כל עוד אין שינויי קוד נוספים. התקנות מקומיות לפיילוט עדיין; בניה לחנויות אחרי ~28.8 |

### כלל ברזל

1. הגרסה לבניה לחנויות אחרי הפיילוט היא **`1.0.9+68`**.
2. אם יתווסף תיקון קוד לפני החנויות — bump ל־`+69` (או הבא) ועדכון המסמך הזה.
3. עד סוף הפיילוט (~28.8): אין בניה מלאה לחנויות; אחריו — בניה מ־`+68` אם לא היה bump.

### בדיקה לפני כל בניה

```bash
cd /path/to/ADL\ ShareFlow
git checkout main && git pull
grep '^version:' mobile/pubspec.yaml          # חייב: 1.0.9+68
git status --porcelain                        # חייב: ריק
```

---

## מה נכנס ל־`+68` (נעול לפיילוט → חנויות)

### הזמנה
- WhatsApp, העתקת קישור, QR, מייל, **קוד הזמנה** (+ אורח בנפרד)
- בלי «שתף בדרך אחרת»

### מטבע (מ־`+67`)
- שרת מחשב שער המרה בכתיבת הוצאה (לא סומך על `rate=1` מהלקוח)
- תיקון הוצאות קיימות עם שער≈1 לפי **תאריך ההוצאה** (`POST /api/adl/expenses/repair-fx`)
- שמירה באפליקציה ממתינה להמרה תקינה לפני שליחה

### אונבורדינג (מ־`+67`)
- מסך כניסה: הרשמה ראשית, כניסה משנית + הסבר ברור

### התראות (מ־`+67`)
- מניעת באנר כפול (מערכת + מקומי) ב־foreground
- טוקן FCM אחד למשתמש+פלטפורמה (מניעת ×3)

---

## מצב ייצור עד סוף הפיילוט (28.8)

| דגל | ערך |
|-----|-----|
| `PAYMENTS_ENABLED` | `false` |
| `PILOT_MODE_ENABLED` | `true` |
