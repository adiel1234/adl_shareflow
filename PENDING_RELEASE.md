# מקור האמת לגרסה — `1.0.9+67`

> אם יש סתירה בין הטלפון / הצ׳אט / הזיכרון לבין המסמך הזה — **רק המסמך + `git` קובעים**.

---

## הזהות של הגרסה (חובה לבניה)

| שדה | ערך מדויק |
|-----|-----------|
| **גרסה לבניה / חנויות / TestFlight** | **`1.0.9+67`** |
| **`mobile/pubspec.yaml` → `version`** | `1.0.9+67` |
| **מספר build** | `67` |
| **שם שיווקי** | 1.0.9 |
| **ענף** | `main` |
| **קומיט התוכן (`+67`)** | יעודכן אחרי הקומיט הנוכחי |
| **גרסה קודמת (`+66`)** | קומיט תוכן `608e444` |
| **מצב נוכחי** | התקנות מקומיות לפיילוט — **עדיין לא** בניה לחנויות |

### כלל ברזל

1. הגרסה לבניה הבאה לחנויות היא **`1.0.9+67`**.
2. אם נוספו תיקוני קוד אחרי קומיט התוכן של `+67` — bump ל־`+68` לפני חנויות.
3. עד סוף הפיילוט (28.8): אין בניה מלאה לחנויות.

### בדיקה לפני כל בניה

```bash
cd /path/to/ADL\ ShareFlow
git checkout main && git pull
grep '^version:' mobile/pubspec.yaml          # חייב: 1.0.9+67
git status --porcelain                        # חייב: ריק
```

---

## מה נכנס ל־`+67`

### מטבע
- שרת מחשב שער המרה בכתיבת הוצאה (לא סומך על `rate=1` מהלקוח)
- תיקון הוצאות קיימות עם שער≈1 לפי **תאריך ההוצאה** (`POST /api/adl/expenses/repair-fx`)
- שמירה באפליקציה ממתינה להמרה תקינה לפני שליחה

### הזמנה
- רק: WhatsApp, העתקת קישור, QR, מייל (+ אורח בנפרד)
- הוסר «שתף בדרך אחרת»

### אונבורדינג
- מסך כניסה: הרשמה ראשית, כניסה משנית + הסבר ברור

### התראות
- מניעת באנר כפול (מערכת + מקומי) ב־foreground
- טוקן FCM אחד למשתמש+פלטפורמה (מניעת ×3)

---

## מצב ייצור עד סוף הפיילוט (28.8)

| דגל | ערך |
|-----|-----|
| `PAYMENTS_ENABLED` | `false` |
| `PILOT_MODE_ENABLED` | `true` |
