# מקור האמת לגרסה — `1.0.9+66`

> אם יש סתירה בין הטלפון / הצ׳אט / הזיכרון לבין המסמך הזה — **רק המסמך + `git` קובעים**.

---

## הזהות של הגרסה (חובה לבניה)

| שדה | ערך מדויק |
|-----|-----------|
| **גרסה לבניה / חנויות / TestFlight** | **`1.0.9+66`** |
| **`mobile/pubspec.yaml` → `version`** | `1.0.9+66` |
| **מספר build** | `66` |
| **שם שיווקי** | 1.0.9 |
| **ענף** | `main` |
| **קומיט התוכן הקודם (`+65`)** | **`bcce496`** |
| **גרסה קודמת מופצת** | `1.0.9+65` (מקומי / טרם חנויות) / `1.0.9+64` = `b767ac8` |

### כלל ברזל

1. הגרסה היא **`1.0.9+66`** אחרי תיקוני UX התראות / סשן / תג אייקון / ניסוחים.
2. לפני בניה לחנויות: `git status` ריק + `version: 1.0.9+66` ב־`pubspec.yaml`.
3. תיקון FCM `badge` בשרת חייב להיות פרוס ל־Railway לפני בדיקת תג על אייקון.

### בדיקה לפני כל בניה

```bash
cd /path/to/ADL\ ShareFlow
git checkout main && git pull
grep '^version:' mobile/pubspec.yaml          # חייב: 1.0.9+66
git status --porcelain                        # חייב: ריק
```

---

## מה נכנס ל־`+66` (תיקוני UX אחרי פיילוט)

### התראות
- הבחנה **פעולה** (כתום/אדום: אישור תשלום, תזכורת חוב, שדרוג, תפוגה) מול **מידע** (כחול/נייטרלי)
- טעינת **כל** עמודי ההתראות (לא נחתך ב־~30)
- תג אייקון = **מספר unread אמיתי** (לא `badge=1` קבוע); איפוס בפתיחה / סמן כנקרא / יציאה

### סשן
- רענון טוקן single-flight (פחות ניתוקי שווא)
- 403 לא מנתק; רק 401 אחרי refresh שנדחה
- מסך התחברות: מילוי שקט של פרטים שמורים (בלי דיאלוג מבלבל)

### ניסוח
- מונח אחיד: **הסדר חוב**
- בלי מרכאות `«»` בממשק
- טיפי חברים מקוצרים (+/− ואורח)
- סיור/howto: אירוע מול שוטף

---

## מצב ייצור עד סוף שבוע הבא

| דגל | ערך |
|-----|-----|
| `PAYMENTS_ENABLED` | `false` |
| `PILOT_MODE_ENABLED` | `true` |

---

## בניה לחנויות (סוף שבוע הבא)

```bash
cd mobile
git checkout main && git pull
grep '^version:' pubspec.yaml   # 1.0.9+66

flutter build ipa --release --dart-define=FLAVOR=prod
flutter build appbundle --release --dart-define=FLAVOR=prod
```

אחרי בניה: לרשום כאן תאריך + `git rev-parse --short HEAD` שממנו נבנה.

---

## צ׳קליסט העלאה

1. [ ] `version` = `1.0.9+66` ו־working tree ריק
2. [ ] Backend עם תיקון badge פרוס ב־Railway
3. [ ] סיום פיילוט: `PILOT_MODE_ENABLED=false`, `PAYMENTS_ENABLED=true`
4. [ ] בניה אחת (IPA + AAB)
5. [ ] Submit + Smoke
