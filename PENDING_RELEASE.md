# מקור האמת לגרסה — `1.0.9+65`

> אם יש סתירה בין הטלפון / הצ׳אט / הזיכרון לבין המסמך הזה — **רק המסמך + `git` קובעים**.

---

## הזהות של הגרסה (חובה לבניה)

| שדה | ערך מדויק |
|-----|-----------|
| **שם גרסה לחנויות / TestFlight** | `1.0.9+65` |
| **`mobile/pubspec.yaml` → `version`** | `1.0.9+65` |
| **מספר build (iOS/Android)** | `65` |
| **שם שיווקי** | 1.0.9 |
| **ענף** | `main` |
| **Commit לבילוי** | `d2d1dd2` (`d2d1dd2` = tip של `main`; כולל את כל התיקונים) |
| **תואם `origin/main`** | כן (אין שינויים מקומיים שלא ב־remote) |
| **קומיט התוכן העיקרי** | `bcce496` — Prepare 1.0.9+65 for store release |
| **גרסה קודמת מופצת לפיילוט** | `1.0.9+64` = `b767ac8` |

### כלל בנייה

לבנות **רק** מ־`main` כש:

```bash
git rev-parse --short HEAD   # חייב: d2d1dd2  (או commit חדש יותר שתועד כאן אחרי bump)
grep '^version:' mobile/pubspec.yaml   # חייב: 1.0.9+65
git status --porcelain               # חייב להיות ריק
```

אם נוספו תיקונים אחרי `d2d1dd2` — **לא** לבנות `+65` שוב; לעשות bump ל־`+66` ולעדכן מסמך זה.

---

## טווח השינויים שנכנסים ל־`+65`

כל מה שבוצע מ־`b767ac8` (`+64`) עד `d2d1dd2`, כולל:

| Commit | תוכן |
|--------|------|
| `532d3b5` | טעינת **כל** עמודי ההוצאות + סה״כ בכותרת קבוצה |
| `bcce496` | כל תיקוני הערב/היום (ראה רשימה למטה) |
| `d10bf2a` / `fc9463e` / `d2d1dd2` | תיעוד: פיילוט נשאר פתוח + נעילת זהות הגרסה |

---

## רשימת תיקונים ושיפורים — חובה שיופיעו בבילד `+65`

### באגים / נכונות נתונים
- [x] באג 30 הוצאות — הלקוח טוען את כל העמודים (`per_page=100` בלולאה), לא רק עמוד אחד
- [x] עיגול כסף — תצוגה/העתקה/WhatsApp עם אגורות (לא עיגול לשקל שלם)
- [x] חלוקה שווה — remainder על המניה האחרונה (בלי 400 עם הרבה חברים)
- [x] סה״כ תקופתי — לא מערבבים lifetime עם «תקופה נוכחית»
- [x] פירוט הוצאות לפי מטבע — בטאב «מי חייב למי» + שורה תחת סה״כ בכותרת קבוצה
- [x] סמל מטבע — `149 ₪` במקום `ILS`
- [x] שגיאות הצטרפות — מציגות הודעת שרת אמיתית (לא תמיד «קוד לא תקין»)

### התראות / הזמנות / אורחים
- [x] FCM iOS — `AppDelegate` + רישום APNs / retry
- [x] קישור אורח בהצטרפות (דמיון שם) — backend + לקוח
- [x] Deep link join בלי מסך 404

### UX / מובייל
- [x] סיור בית + סיור קבוצה + howto
- [x] יצירת קבוצה: תמחור גלוי, מעבר לקבוצה + הזמנה
- [x] טאב «מי חייב למי», FAB רק בהוצאות, באנר פרטי תשלום
- [x] אנדרואיד — Safe Area / לא מכסה את התחתית
- [x] מונה «מציג X הוצאות» ברשימה
- [x] אייקון בנק נראה במסך תשלום

### תשלומים (קוד מוכן; דגל כבוי בפיילוט)
- [x] מיפוי מוצרי IAP כולל 5/10/25
- [x] הודעות כשל ברורות כש־IAP לא מצליח
- [x] אימות Google Play עם package נכון (`com.adl.shareflow`)
- [ ] **הפעלת חיוב בחנויות** — רק בסוף שבוע הבא (`PAYMENTS_ENABLED=true`)

### תשתית
- [x] Backend של התיקונים פרוס ב־Railway
- [x] ניקוי נתוני הדמייה/עומס מפיילוט (לא חלק מהבילד — פעולת DB)

---

## מצב ייצור עכשיו (לא חלק ממספר הגרסה)

| דגל | ערך | עד מתי |
|-----|-----|--------|
| `PAYMENTS_ENABLED` | `false` | עד העלאה לחנויות |
| `PILOT_MODE_ENABLED` | `true` | עד סיום פיילוט (סוף שבוע הבא) |

הבילד `+65` **מכיל** את קוד התשלומים; החיוב נדלק בדשבורד בנפרד.

---

## בניה — פקודות מדויקות

```bash
cd mobile
git checkout main
git pull
git rev-parse --short HEAD    # → fc9463e (או חדש יותר שתועד כאן)
grep '^version:' pubspec.yaml # → 1.0.9+65
git status --porcelain        # → ריק

flutter build ipa --release --dart-define=FLAVOR=prod
flutter build appbundle --release --dart-define=FLAVOR=prod
```

אחרי בניה מוצלחת — לרשום כאן: תאריך הבניה, ושה־IPA/AAB נבנו מ־`fc9463e` / `1.0.9+65`.

---

## צ׳קליסט העלאה (סוף שבוע הבא)

1. [ ] וידוא שאין commits חדשים שלא תועדו (או bump ל־`+66`)
2. [ ] `HEAD` + `version` תואמים לטבלת הזהות למעלה
3. [ ] סיום פיילוט בדשבורד: `PILOT_MODE_ENABLED=false`, `PAYMENTS_ENABLED=true`
4. [ ] בניה אחת לפי הפקודות למעלה
5. [ ] Submit + Smoke

---

## פקודות אימות מהירות

```bash
git rev-parse --short HEAD
git status --porcelain
grep '^version:' mobile/pubspec.yaml
curl -s https://adlshareflow-production.up.railway.app/api/config/public
```
