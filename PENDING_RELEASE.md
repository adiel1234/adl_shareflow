# מקור האמת לגרסה — `1.0.9+66`

> אם יש סתירה בין הטלפון / הצ׳אט / הזיכרון לבין המסמך הזה — **רק המסמך + `git` קובעים**.

---

## הזהות של הגרסה (חובה לבניה)

| שדה | ערך מדויק |
|-----|-----------|
| **גרסה לבניה / חנויות / TestFlight** | **`1.0.9+66`** |
| **`mobile/pubspec.yaml` → `version`** | `1.0.9+66` |
| **מספר build** | `66` |
| **שם שיווקי** | 1.0.9 |
| **ענף** | `main` |
| **קומיט התוכן (כל תיקוני `+66`)** | **`608e444`** — *clear app badge on mark-read and remove false 25-cap copy* |
| **מינימום לבילוי** | כל commit ב־`main` ש־`608e444` הוא אב שלו, ו־`pubspec` עדיין `1.0.9+66` |
| **גרסה קודמת (`+65`)** | קומיט תוכן `bcce496` (לפני תיקוני UX/תג) |
| **גרסה מופצת קודם** | `1.0.9+64` = `b767ac8` |
| **מצב נוכחי** | מותקן מקומית על אייפון הפיילוט בלבד — **עדיין לא** בניה לחנויות |

### כלל ברזל

1. הגרסה לבניה הבאה לחנויות היא **`1.0.9+66`** — זה מה שיופיע ב־App Store Connect / Play / על המכשיר.
2. היא **חייבת** לכלול את קומיט התוכן **`608e444`** (ומה שאחריו ב־`main` עד tip, כל עוד לא שינינו קוד בלי bump).
3. אם נוספו **תיקוני קוד** אחרי `608e444` — חובה bump ל־`1.0.9+67` (או יותר) **לפני** בניה לחנויות, ולעדכן מסמך זה.
4. קומיטי תיעוד בלבד אחרי `608e444` **לא** דורשים bump — עדיין בונים כ־`+66`.
5. עד סוף הפיילוט: אין בניה מלאה לחנויות; רק התקנות מקומיות על מכשירי בדיקה.

### בדיקה לפני כל בניה

```bash
cd /path/to/ADL\ ShareFlow
git checkout main && git pull
grep '^version:' mobile/pubspec.yaml          # חייב: 1.0.9+66
git merge-base --is-ancestor 608e444 HEAD && echo "OK: 608e444 included"
git status --porcelain                        # חייב: ריק
```

---

## מה נכנס ל־`+66`

טווח: אחרי `bcce496` (`+65`) עד tip שכולל **`608e444`**.

### התראות
- הבחנה **פעולה** (כתום/אדום: אישור תשלום, תזכורת חוב, שדרוג, תפוגה) מול **מידע** (כחול/נייטרלי)
- טעינת **כל** עמודי ההתראות (לא נחתך ב־~30)
- תג אייקון = **מספר unread אמיתי** (לא `badge=1` קבוע)
- איפוס תג: `app_badge_plus` + פוש badge-only מהשרת אחרי סמן כנקרא / סמן הכל / יציאה
- endpoints אדמין: `demo-notifications`, `clear-badge`

### סשן
- רענון טוקן single-flight (פחות ניתוקי שווא)
- 403 לא מנתק; רק 401 אחרי refresh שנדחה
- מסך התחברות: מילוי שקט של פרטים שמורים (בלי דיאלוג מבלבל)

### סיור וניסוח
- סיור **אחד רציף** בית + קבוצה (מונה צעדים משותף)
- הסבר **אירוע / שוטף** במסך יצירה + howto — לא בתחילת סיור הכפתורים
- הוסר ניסוח שגוי «עד 25 משתתפים» מאירוע (אין מגבלה כזו בקוד)
- מונח אחיד: **הסדר חוב**
- בלי מרכאות `«»` בממשק
- טיפי חברים מקוצרים (+/− ואורח)

### תשתית
- Backend פרוס ב־Railway עם תיקוני FCM badge
- ייצור עדיין בפיילוט (תשלומים כבויים)

---

## מצב ייצור עד סוף הפיילוט

| דגל | ערך |
|-----|-----|
| `PAYMENTS_ENABLED` | `false` |
| `PILOT_MODE_ENABLED` | `true` |

בניה לחנויות — **רק בסוף הפיילוט** (סוף שבוע הבא / לפי החלטה), לא לפני.

---

## בניה לחנויות (בסוף הפיילוט)

```bash
cd mobile
git checkout main && git pull
grep '^version:' pubspec.yaml   # 1.0.9+66
git merge-base --is-ancestor 608e444 HEAD && echo OK

flutter build ipa --release --dart-define=FLAVOR=prod
flutter build appbundle --release --dart-define=FLAVOR=prod
```

אחרי בניה: לרשום כאן תאריך + `git rev-parse --short HEAD` שממנו נבנה.

---

## צ׳קליסט העלאה

1. [ ] `version` = `1.0.9+66` ו־`608e444` כלול ב־HEAD
2. [ ] working tree ריק
3. [ ] Backend (badge sync) פרוס ב־Railway
4. [ ] סיום פיילוט: `PILOT_MODE_ENABLED=false`, `PAYMENTS_ENABLED=true`
5. [ ] בניה אחת (IPA + AAB)
6. [ ] Submit + Smoke
